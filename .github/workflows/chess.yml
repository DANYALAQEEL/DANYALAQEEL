{"name":"Chess","on":["push","issue_comment"],"permissions":{"contents":"write","issues":"write"},"jobs":{,"steps":[{"uses":"actions/checkout@v3"},{"run":"git clone https://github.com/marcizhu/readme-chess.git e && cp -r e/src e/data e/games e/img e/main.py e/requirements.txt . && pip install -r requirements.txt && python main.py","env":{"GITHUB_TOKEN":"${{ secrets.GITHUB_TOKEN }}","ISSUE_NUMBER":"${{ github.event.issue.number }}"}},{"run":"git config user.name bot && git config user.email bot@bot.com && git add . && git commit -m move || true && git push"}]}}}
"run":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v3"},{"run":"git clone https://github.om/marcizhu/readme-chess.git e && cp -r e/src e/data e/games e/img e/main.py e/requirements.txt . && pip install -r requirements.txt && python main.py","env":{"GITHUB_TOKEN":"${{ secrets.GITHUB_TOKEN }}","ISSUE_NUMBER":"${{ github.event.issue.number }}"}},{"run":"git config user.name bot && git config user.email bot@bot.com && git add . && git commit -m move || true && git push"}]}}}
com/marcizhu/readme-chess.git e && cp -r e/src e/data e/games e/img e/main.py e/requirements.txt . && pip stall -r requirements.txt && python main.py","env":{"GITHUB_TOKEN":"${{ secrets.GITHUB_TOKEN }}","ISSUE_NUMBER":"${{ github.event.issue.number }}"}},{"run":"git config user.name bot && git config user.email bot@bot.com && git add . && git commit -m move || true && git push"}]}}}
install -r requirements.txt && python main.py","env":{"GITHUB_TOKEN":"${{ secrets.GITHUB_TOKEN }}","ISSUE_ github.event.issue.number }}"}},{"run":"git config user.name bot && git config user.email bot@bot.com && git add . && git commit -m move || true && git push"}]}}}
NUMBER":"${{ github.event.issue.number }}"}},{"run":"git config user.name bot && git config user.email bot@bot.com && git add . && git commit -m move || true && git push"}]}}}
